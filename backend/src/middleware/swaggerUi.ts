// import swaggerUi from 'swagger-ui-express';
import * as swaggerDocument from '../models/swagger.json';

// app.use('/api-docs', swaggerUi.serve, swaggerUi.setup(swaggerDocument));

export default swaggerDocument;
